@layout('layout/app')

@section('content')
  <h2>{{ task.title }}</h2>

  <div class="card">
    <div class="card-body">
      @if(task.description)
        <p>{{ task.description }}</p>
      @endif
      <p>Feita: {{ task.done ? 'Sim' : 'Não' }}</p>
    </div>
  </div>
  <div>
    <a href="{{ route('lists.tasks.index', { list_id: list.id }) }}" class="btn btn-link">Voltar</a>
  </div>
@endsection
